name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build and push Docker image
        run: |
          docker build -t mydockerhubusername/myapp:latest -f docker/Dockerfile .
          docker tag mydockerhubusername/myapp:latest mydockerhubusername/myapp:${{ github.sha }}
          docker push mydockerhubusername/myapp:latest
          docker push mydockerhubusername/myapp:${{ github.sha }}
